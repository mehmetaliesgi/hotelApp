<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
                                <%= render "devise/shared/error_messages", resource: resource %>
                                 <div class="form-group">
                                    <%= f.text_field :tckn, class: "form-control form-control-user", placeholder: "T.C. Kimlik No" %>
                                </div>
                                <div class="form-group row">
                                    <div class="col-sm-6 mb-3 mb-sm-0">
                                        <%= f.text_field :name, class: "form-control form-control-user", placeholder: "First Name" %>
                                    </div>
                                    <div class="col-sm-6">
                                        <%= f.text_field :surname, class: "form-control form-control-user", placeholder: "Last Name" %>
                                    </div>
                                </div>
                                <div class="form-group">
                                   <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control form-control-user", placeholder: "Email Address" %>
                                </div>
                                 <div class="form-group row">
                                    <div class="col-sm-6 mb-3 mb-sm-0">
                                        <%= f.text_field :phone, class: "form-control form-control-user", placeholder: "Phone Number" %>
                                    </div>
                                    <div class="col-sm-6">
                                        <%= f.text_field :phone2, class: "form-control form-control-user", placeholder: "Phone Number 2 (Optional)" %>
                                    </div>
                                </div>
                                <div class="form-group row">
                                    <div class="col-sm-6 mb-3 mb-sm-0">
                                        <% if @minimum_password_length %>
                                        <em>(<%= @minimum_password_length %> characters minimum)</em>
                                        <% end %><br />
                                        <%= f.password_field :password, autocomplete: "new-password", class: "form-control form-control-user", placeholder: "Password" %>
                                    </div>
                                    <div class="col-sm-6">
                                        <% if @minimum_password_length %>
                                        <em>(<%= @minimum_password_length %> characters minimum)</em>
                                        <% end %><br />
                                        <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "form-control form-control-user", placeholder: "Repeat Password" %>
                                    </div>
                                </div>
                                
                                <%= button_to 'Gönder', new_user_registration_path ,class: 'btn btn-primary btn-user btn-block' %>
                            
<% end %>